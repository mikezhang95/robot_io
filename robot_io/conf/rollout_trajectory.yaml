defaults:
  - env: env
  - robot: panda_frankx_interface_policy
  - recorder: simple_recorder
  - cams: no_cameras
  - override hydra/job_logging: colorlog
  - override hydra/hydra_logging: colorlog
  - _self_

hydra:
  run:
    dir: ${save_dir}/${now:%Y-%m-%d}/${now:%H-%M-%S}

save_dir: tmp/reach_rollouts
load_dir: ???

use_rel_actions: False
rel_action_type: aa

env:
  freq: 30
